node_modules
.next
.env
.env.local

.vercel
.env*

# Generated at build time by @ducanh2912/next-pwa (workbox-webpack-plugin) —
# regenerated on every `next build`, not source.
public/sw.js
public/sw.js.map
public/swe-worker*.js
public/swe-worker*.js.map
public/workbox-*.js
public/workbox-*.js.map
# Prompt 2's custom worker (worker/index.ts, customWorkerPrefix "worker" in
# next.config.mjs) — same "regenerated every build, not source" posture as
# the rest of this block, just a filename pattern the existing rules above
# didn't happen to cover.
public/worker-*.js
public/worker-*.js.map

# Local Supabase CLI cache, not source.
supabase/.temp/

# Per-session lock file from the Claude Code harness, not source.
.claude/scheduled_tasks.lock
